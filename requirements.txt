# 无人机 RID 接收系统 - 依赖锁定文件
# 生成: pip freeze > requirements.txt (安装所有可选依赖后执行)
# 安装: pip install -r requirements.txt
#
# 核心依赖
pyyaml==6.0.2
python-dotenv==1.1.1
flask>=3.0.0
#
# 可选: BLE 扫描 (需要蓝牙硬件)
# bleak>=0.21.0
#
# 可选: WiFi Beacon 扫描 (Windows 需 Npcap, Linux 需 monitor mode)
# scapy>=2.5.0
#
# 开发依赖
pytest>=8.0
pyinstaller>=6.0

# 可选: 数据回传 (HTTP 上报)
requests>=2.28.0

# 可选: 阿里云短信 (专责人员告警通知)
# alibabacloud_dysmsapi20170525>=3.0.0
# alibabacloud_tea_openapi>=0.3.0
# alibabacloud_tea_util>=0.3.0
